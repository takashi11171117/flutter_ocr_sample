import 'package:flutter/material.dart';

class MlkitSample extends StatelessWidget {
  const MlkitSample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TesseractOcr デモ'),
      ),
      body: Text('サンプル'),
    );
  }
}
